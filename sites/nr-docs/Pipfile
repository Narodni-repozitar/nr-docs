[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
check-manifest = ">=0.25"

[packages]
oarepo = { version = "~=11.0.0", extras = [] }
oarepo-ui = ">=4.0.10"
nr-docs = {editable="True", path="./site"}

uwsgi = ">=2.0"
uwsgitop = ">=0.11"
uwsgi-tools = ">=1.1.1"
oarepo-vocabularies = ">=1.0.0"
deepmerge = ">=1.1.0"
nr-vocabularies = ">=1.0.0"
invenio-records-resources = "==1.0.9"


nr-documents = {editable = true, path = "../../models/nr-documents"} # Added by nrp-cli


docs-app = {editable = true, path = "../../ui/docs-app"} # Added by nrp-cli

[requires]
# python_version = "3.9"    -- version check removed as we would like to be able to try tests for python3.10+

[pipenv]
allow_prereleases = false
