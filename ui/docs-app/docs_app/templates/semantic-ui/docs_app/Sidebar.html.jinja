{#def metadata #}
<section aria-label="{{_('Collection')}}" class="ui segment">
  <h2 class="ui small header">{{_('Collection')}}</h2>
</section>
<section aria-label="{{_('metadata/objectIdentifiers.label')}}" class="ui segment">
  <h2 class="ui small header">{{_('metadata/objectIdentifiers.label')}}</h2>
  <div class="ui link list">
    <div class="item">
      <i class="external alternate icon"></i>

    </div>
  </div>
</section>
<section id="export-record" aria-label="{{_('Export')}}" class="ui segment exports">
  <h2 class="ui small header">{{_('Export')}}</h2>
  <div id="recordExportDownload">
    <div class="ui grid">
      <div class="eleven wide column">
        <div
          role="listbox"
          aria-expanded="false"
          class="ui fluid selection dropdown"
          tabindex="0"
        >
          <div
            aria-atomic="true"
            aria-live="polite"
            role="alert"
            class="divider text"
          >
            JSON
          </div>
          <i aria-hidden="true" class="dropdown icon"></i>
          <div class="menu transition">
            <div
              style="pointer-events: all;"
              role="option"
              aria-checked="true"
              aria-selected="true"
              class="active selected item"
            >
              <span class="text">JSON</span>
            </div>
          </div>
        </div>
      </div>
      <div class="five wide column pl-0">

          Export
        </a>
      </div>
    </div>
  </div>
</section>

<section aria-label="API Links" class="ui segment">
  <h2 class="ui small header">{{_('API')}} {{_('Links')}}</h2>
</section>
